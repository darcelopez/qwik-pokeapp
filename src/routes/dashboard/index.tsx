import { Slot, component$ } from '@builder.io/qwik';

export default component$(() => {
  

    return(
        <>
            <h3>Dashboard </h3>

            <p>Esta ruta debe ser privada</p>

        </>
    )
});